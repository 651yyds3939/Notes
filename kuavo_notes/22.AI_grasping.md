你一针见血地指出了问题，这确实是我的架构失误。

你说得完全正确。你花了整整一章的精力在 Orin NX 上硬核排障，强行降级安装了匹配的 `cuDNN 8.6.0`，并使用专供 Tegra 的 `.whl` 编译了 PyTorch。如果把 YOLO 节点挪到下位机 i9 上，由于缺乏这套量身定制的张量加速环境，必然会退化到纯 CPU 推理，导致单帧耗时暴涨到 400-800ms，彻底击穿具身智能“零延迟”的底线。

**终端 5 必须死死钉在上位机（Orin NX）上，绝不能动。**

这意味着，我们要让 Orin NX 在 7.3G 的物理内存红线内，同时扛起 **YOLOv8（视觉）** 和 **Faster-Whisper（听觉）** 这两座大山。好在你已经在实战中执行了 `sudo systemctl isolate multi-user.target` 剥离了 GUI 桌面，并且将图像队列压缩到了 `queue_size=1`，这为你抠出了极其宝贵的 1GB+ 纯血显存，使得“双模态并发”在物理上成为了可能。

基于你的纠正，这是为你重新梳理的**修正版 9 终端全链路点火流水线**。

---

### 🟢 阵列一：下位机运动底盘组 (SSH 登录 i9 节点)

*核心哲学：优先拉起底盘控制与数学引擎，为后续的动作提供物理支撑。*

**🖥️ 终端 1：唤醒 WBC 全身力矩控制**

```bash
ssh lab@192.168.26.1
sudo su
cd kuavo-ros-opensource
source devel/setup.bash
roslaunch humanoid_controllers load_kuavo_real.launch cali:=true

```

*(必须等待标定完成，机器人站稳)*

**🖥️ 终端 2：启动 IK 逆运动学小脑**

```bash
ssh lab@192.168.26.1
sudo su
cd kuavo-ros-opensource
source devel/setup.bash
roslaunch motion_capture_ik ik_node.launch

```

*(为 3D 避障规划提供数学解算)*

**🖥️ 终端 3：物理锁定头部基准**

```bash
ssh lab@192.168.26.1
sudo su
cd kuavo-ros-opensource
source devel/setup.bash
python3 src/demo/vla_grasp/look_down.py

```

*(让头部探至物理极限最低点，稳固光学坐标)*

---

### 🔵 阵列二：下位机认知与执行组 (SSH 登录 i9 节点)

*核心哲学：i9 只负责吃内存的大语言模型（Qwen2-7B）、语音合成服务，以及接收指令后的机械臂控制。*

**🖥️ 终端 4：挂起纯 Python 离线发声器官 (TTS)**

```bash
ssh lab@192.168.26.1
python3 /home/lab/tts_server.py

```

*(监听 5000 端口，随时准备发声)*

**🖥️ 终端 5：启动 VLA 守护态抓取小脑**

```bash
ssh lab@192.168.26.1
sudo su
cd kuavo-ros-opensource
source devel/setup.bash
# 运行我们前面重构的守护进程，死盯 /vla/master_command 话题
python3 src/demo/vla_grasp/vla_auto_grasp_daemon.py 

```

---

### 🟠 阵列三：上位机双模态满血感知组 (本地直接运行)

*核心哲学：Orin NX 火力全开，利用解封的 GPU 算力同时并行 ASR 听觉与 YOLO 视觉。*

**🖥️ 终端 6：睁开深度相机之眼**

```bash
cd ~/kuavo_ros_application
source devel/setup.bash
roslaunch dynamic_biped load_robot_head.launch

```

*(向底层总线广播深度与彩色图像)*

**🖥️ 终端 7：启动 YOLO + TF2 视觉中枢**

```bash
cd ~/kuavo_ros_application
source devel/setup.bash
# 你的心血代码：调用 float16 算力，结合 TF2 消除头部晃动，发布绝对坐标
python3 src/ros_vision/detection_industrial_yolo/yolo_box_object_detection/scripts/yolo_box_segment_ros_TF2.py

```

*(此时 YOLO 开始在后台以 20FPS 刷出 TF2_X, TF2_Y 坐标)*

**👁️ 终端 8：启动 RQT 工程师上帝视角 (⚠️ OOM 高危预警)**

```bash
source /opt/ros/noetic/setup.bash
rqt_image_view

```

*(**极客建议**：`rqt` 极度消耗内存。如果此时 Orin 报 OOM 闪退，请关闭此终端。直接在你的笔记本上配置 `ROS_MASTER_URI` 指向 Orin NX，在笔记本端开 RQT 监视，将 Orin NX 的内存彻底省给大模型！)*

**🔥 终端 9：启动终极 VLA 主控大脑 (结合了 Whisper 的代码)**

```bash
cd ~/kuavo_ros_application
source devel/setup.bash
# 启动内置了 Faster-Whisper ASR 并通过 HTTP 调度 Qwen2-7B 的 Python 脚本
python3 src/kuavo_large_model/rtasr_python3_demo.py

```

*(此时终端开始跳动音量雷达)*

上位机主控与听觉引擎 (`rtasr_python3_demo.py`)

**部署位置**：上位机 Jetson Orin NX (ARM aarch64, IP: `192.168.26.12`)
**核心使命**：负责物理级高频监听、动态雷达降噪、GPU 满血并行 ASR 转写、跨主板 HTTP 神经调度。

```python
# -*- encoding:utf-8 -*-
import os
import sys
import time
import wave
import audioop
import pyaudio
import requests
import json
from faster_whisper import WhisperModel

# 🌟 引入 ROS 和 String 消息
import rospy
from std_msgs.msg import String

# ================= 1. 物理音频捕获层配置 =================
FORMAT = pyaudio.paInt16
CHANNELS = 1
RATE = 16000     
CHUNK = 1024     

THRESHOLD = 4000         
SILENCE_DURATION = 0.8   
RECORD_SECONDS = 60      
INPUT_FILE = "input.wav" 

LOWER_COMPUTER_IP = "192.168.26.1" 
global_audio = pyaudio.PyAudio()

# 全局变量占位
vla_cmd_pub = None

print("==================================================")
print("正在加载本地 Faster-Whisper 语音识别模型...")

# ================= 2. GPU 算力引擎初始化 =================
whisper_model = WhisperModel(
    "/home/leju_kuavo/faster-whisper-small",  
    device="cuda",                            
    compute_type="float16",                   
    local_files_only=True                     
)
print("本地语音识别系统初始化成功！")
print("==================================================")


def chat():
    """麦克风阵列底层监听与 VAD（语音端点检测）逻辑"""
    stream = global_audio.open(format=FORMAT, channels=CHANNELS, rate=RATE, input=True, frames_per_buffer=CHUNK)
    print("\n>>> 【夸父】正在倾听，请开始提问...")

    frames = []
    recording = False
    silence_start = None
    start_time = time.time()
    radar_counter = 0

    while not rospy.is_shutdown():  # 🌟 加入 ROS 节点存活判断
        data = stream.read(CHUNK, exception_on_overflow=False)
        rms = audioop.rms(data, 2)  
        
        radar_counter += 1
        if radar_counter % 5 == 0 and not recording:
            print(f"📊 [音量雷达] 当前环境音量: {rms:4d}  (触发门槛: {THRESHOLD})", end='\r')
        
        if rms > THRESHOLD:
            if not recording:
                recording = True
                print("\n-> 🎙️ 突破门槛！检测到有效人声，开始录音...")
            frames.append(data)
            silence_start = None  
        else:
            if recording:
                frames.append(data)  
                if silence_start is None:
                    silence_start = time.time()
                elif time.time() - silence_start > SILENCE_DURATION:
                    print("-> 🛑 说话结束，正在分析文本...")
                    break

        if time.time() - start_time > RECORD_SECONDS:
            break

    stream.stop_stream()
    stream.close()

    with wave.open(INPUT_FILE, 'wb') as wf:
        wf.setnchannels(CHANNELS)
        wf.setsampwidth(global_audio.get_sample_size(FORMAT))
        wf.setframerate(RATE)
        wf.writeframes(b''.join(frames))


def local_stt(file_path):
    """GPU 音频特征解码与幻觉斩杀层"""
    try:
        segments, info = whisper_model.transcribe(
            file_path, 
            beam_size=5,                          
            language="zh",
            initial_prompt="这是一段日常对话。",     
            condition_on_previous_text=False
        )
        
        text = ""
        for segment in segments:
            if segment.no_speech_prob <= 0.8:
                text += segment.text
                
        return text.strip()
    except Exception as e:
        print(f"【耳朵报错】: {e}")
        return ""


def local_offline_closed_loop_chat(text):
    """跨端 HTTP 神经调度中枢：升级为 VLA 具身动作架构"""
    global vla_cmd_pub  # 🌟 声明全局变量，确保能调用到底层的 ROS Publisher
    
    ollama_url = f"http://{LOWER_COMPUTER_IP}:11434/api/chat"
    
    vla_system_prompt = """
    你叫夸父，是乐聚机器人研发的人形具身智能机器人。
    你现在必须作为一个【JSON状态机】运行，严格根据人类指令输出动作意图。
    
    【输出格式约束】
    你必须且只能输出合法的 JSON 格式，绝不允许包含任何 Markdown 符号（如 ```json）或其他说明性文字。
    JSON 必须包含以下三个字段：
    1. "thought": 你的推理过程（分析人类意图）。
    2. "action_cmd": {"action": "grab", "target": "目标物体名称"}。如果不需要动作，设为 {"action": "idle", "target": "none"}。
    3. "tts_reply": 你用来回答人类的话。字数在30字以内。
    
    【绝密发声指令】
    你的 "tts_reply" 字段内容将直接送入纯中文语音合成引擎。因此该字段内【绝对不能】包含任何阿拉伯数字（如1、2）和英文字母（如AI、OK）。必须将其翻译为纯中文汉字（例如'172'写为'一百七十二'）。
    """

    ollama_payload = {
        "model": "qwen2:7b",
        "format": "json", 
        "messages": [
            {"role": "system", "content": vla_system_prompt},
            {"role": "user", "content": text}
        ],
        "stream": False
    }

    try:
        print(f"  [人类指令]: {text}")
        
        # --- 1. 第一发突击：呼叫 NUC 上的 Qwen2-7B 大脑 ---
        ollama_response = requests.post(ollama_url, json=ollama_payload, timeout=60)
        ollama_response.raise_for_status() 
        raw_reply = ollama_response.json()["message"]["content"]
        
        # --- 2. JSON 解析与防御性兜底 ---
        try:
            brain_state = json.loads(raw_reply)
            thought = brain_state.get("thought", "思考异常")
            action_cmd = brain_state.get("action_cmd", {"action": "idle", "target": "none"})
            tts_reply = brain_state.get("tts_reply", "好的")
        except json.JSONDecodeError:
            print(f" [错误] 大模型 JSON 格式失控: {raw_reply}")
            action_cmd = {"action": "idle", "target": "none"}
            tts_reply = "系统认知模块出现异常，我需要重新思考。"

        print(f"  [夸父思维]: {thought}")
        print(f"  [机器指令]: 执行动作 -> {action_cmd['action']}, 目标 -> {action_cmd['target']}")
        print(f"  [语音反馈]: {tts_reply}")
        
        # --- 3. 第二发突击：将纯正中文汉字打给 NUC 上的 TTS 引擎 ---
        print("-> 🧠 正在驱动语音与底层运动模块...")
        tts_url = f"http://{LOWER_COMPUTER_IP}:5000/tts"
        requests.post(tts_url, json={"text": tts_reply}, timeout=60) 
        
        # --- 4. 神经总线下发：向 ROS 运动小脑发送触发信号 ---
        if action_cmd["action"] == "grab" and vla_cmd_pub is not None:
            print(f"-> ⚡ 触发物理抓取管线：目标 [{action_cmd['target']}]")
            # 将 JSON 机器指令序列化为字符串，打向 ROS 总线
            vla_cmd_pub.publish(json.dumps(action_cmd))

        print("-> ✅ 交互闭环完成！")
        
    except requests.exceptions.RequestException as err:
        print(f"【局域网通信失败】: {err}")
    except Exception as e:
        print(f"【发生未知错误】: {e}")


# ================= 主控制流 =================
if __name__ == '__main__':
    print("\n==================================================")
    print("      Kuavo 100% 本地全离线具身交互系统已就绪       ")
    print("==================================================\n")
    
    # 🌟 必须设置 disable_signals=True，否则 ROS 节点会劫持 Ctrl+C 导致 pyaudio 崩溃
    rospy.init_node('vla_brain_commander', disable_signals=True)
    vla_cmd_pub = rospy.Publisher('/vla/master_command', String, queue_size=1)
    
    try:
        # 🌟 修改为 ROS 标准存活循环
        while not rospy.is_shutdown():
            chat()
            sentence = local_stt(INPUT_FILE)
            
            clean_sentence = sentence.replace("。", "").replace("，", "").replace("！", "").replace("？", "").strip()
            
            if clean_sentence:
                local_offline_closed_loop_chat(sentence)
            else:
                print("-> 📭 录音空白或环境杂音，重新倾听。")
                
    except KeyboardInterrupt:
        print("\n[!] 收到退出指令，安全关闭音频流。")
        global_audio.terminate()
        # 手动通知 ROS 关闭
        rospy.signal_shutdown("User KeyboardInterrupt")
        sys.exit(0)

```


### 💻 完整源码：`auto_grasp_TF2.py` (降级防死锁 + 完美自动松爪版)

*(请部署于下位机机器人系统内部)*

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import rospy
import time
import math
import numpy as np
import json
import requests
from std_msgs.msg import String
from geometry_msgs.msg import PointStamped

try:
    from kuavo_msgs.srv import changeArmCtrlMode, changeArmCtrlModeRequest
    from kuavo_msgs.srv import twoArmHandPoseCmdSrv
    from kuavo_msgs.msg import twoArmHandPoseCmd, ikSolveParam
    from kuavo_msgs.msg import armTargetPoses
    from kuavo_msgs.msg import robotHeadMotionData
    from kuavo_msgs.srv import controlLejuClaw, controlLejuClawRequest
except ImportError:
    print("❌ 找不到 kuavo_msgs，请检查环境！")
    exit(1)

# =================================================================
# 🎯 TF2 专属 TCP (夹爪工具中心点) 与 避障通道配置区 (严格保留你的硬核参数)
# =================================================================
TCP_OFFSET_X = 0.005  
TCP_OFFSET_Y_LEFT = 0.03    
TCP_OFFSET_Y_RIGHT = 0.03  
SAFE_LOCKED_Z = 0.37  
LIFT_HEIGHT = 0.20          
AVOID_OUTWARD_Y = 0.10      
CLAW_ROLL_RIGHT = 1.5708  
CLAW_ROLL_LEFT = -1.5708  
# =================================================================

# --- 纯数学推演工具函数保持独立，确保矩阵算力不受影响 ---
class Quaternion:
    def __init__(self):
        self.w = 0; self.x = 0; self.y = 0; self.z = 0

def euler_to_rotation_matrix(yaw, pitch, roll):
    cy, sy = np.cos(yaw), np.sin(yaw)
    cp, sp = np.cos(pitch), np.sin(pitch)
    cr, sr = np.cos(roll), np.sin(roll)
    R_yaw = np.array([[cy, -sy, 0], [sy, cy, 0], [0, 0, 1]])
    R_pitch = np.array([[cp, 0, sp], [0, 1, 0], [-sp, 0, cp]])
    R_roll = np.array([[1, 0, 0], [0, cr, -sr], [0, sr, cr]])
    return R_yaw @ R_pitch @ R_roll

def rotation_matrix_to_quaternion(R):
    trace = np.trace(R)
    q = Quaternion()
    if trace > 0:
        q.w = math.sqrt(trace + 1.0) / 2
        q.x = (R[2, 1] - R[1, 2]) / (4 * q.w)
        q.y = (R[0, 2] - R[2, 0]) / (4 * q.w)
        q.z = (R[1, 0] - R[0, 1]) / (4 * q.w)
    else:
        i = np.argmax([R[0, 0], R[1, 1], R[2, 2]])
        j, k = (i + 1) % 3, (i + 2) % 3
        t = np.zeros(4)
        t[i] = math.sqrt(R[i, i] - R[j, j] - R[k, k] + 1) / 2
        t[j] = (R[i, j] + R[j, i]) / (4 * t[i])
        t[k] = (R[i, k] + R[k, i]) / (4 * t[i])
        t[3] = (R[k, j] - R[j, k]) / (4 * t[i])
        q.x, q.y, q.z, q.w = t
    norm = math.sqrt(q.w**2 + q.x**2 + q.y**2 + q.z**2)
    if norm > 0: q.w /= norm; q.x /= norm; q.y /= norm; q.z /= norm
    return q

def get_horizontal_claw_quat(target_x, target_y, is_left_arm):
    robot_zero_x = -0.017  
    robot_zero_y = 0.292 if is_left_arm else -0.292  
    yaw = math.atan2((target_y - robot_zero_y), (target_x - robot_zero_x))
    R_base = euler_to_rotation_matrix(yaw, -1.57079633, 0)
    roll_angle = CLAW_ROLL_LEFT if is_left_arm else CLAW_ROLL_RIGHT
    cr, sr = math.cos(roll_angle), math.sin(roll_angle)
    R_local_z = np.array([[cr, -sr, 0], [sr, cr, 0], [0, 0, 1]])
    return rotation_matrix_to_quaternion(R_base @ R_local_z)

# --- VLA 守护态状态机核心类 ---
class VLAGraspStateMachine:
    def __init__(self):
        rospy.init_node('vla_auto_grasp_daemon', anonymous=True)
        print("========== 🧠 VLA 守护态运动小脑正在初始化 ==========")
        
        # 1. 切换手臂控制模式
        rospy.wait_for_service('/arm_traj_change_mode')
        rospy.ServiceProxy('/arm_traj_change_mode', changeArmCtrlMode)(changeArmCtrlModeRequest(control_mode=2))
        
        # 2. 初始化底层硬件接口
        self.arm_pub = rospy.Publisher('/kuavo_arm_target_poses', armTargetPoses, queue_size=10)
        self.head_pub = rospy.Publisher('/robot_head_motion_data', robotHeadMotionData, queue_size=10)
        
        rospy.wait_for_service('/ik/two_arm_hand_pose_cmd_srv')
        self.ik_client = rospy.ServiceProxy('/ik/two_arm_hand_pose_cmd_srv', twoArmHandPoseCmdSrv)
        
        # 3. 状态锁，防止大模型抽风并发指令导致机械臂撕裂
        self.is_working = False
        
        # 4. 监听大模型最高指令通道
        rospy.Subscriber('/vla/master_command', String, self.command_callback)
        print("🟢 VLA 运动小脑已挂起！死盯大模型指令中...")

    # --- 底层硬件控制封装 ---
    def call_leju_claw(self, pos, vel, effort):
        try:
            req = controlLejuClawRequest()
            req.data.name = ['left_claw', 'right_claw']
            req.data.position = pos; req.data.velocity = vel; req.data.effort = effort
            rospy.wait_for_service('/control_robot_leju_claw', timeout=3.0)
            rospy.ServiceProxy('/control_robot_leju_claw', controlLejuClaw)(req)
            return True
        except Exception as e: 
            return False

    def solve_ik(self, ik_req, step_name):
        print(f"⏳ 正在计算: {step_name} ...")
        res = self.ik_client(ik_req)
        if not res.success:
            print(f"❌ {step_name} -> IK 解算失败！")
            return False, None
        print(f"✅ {step_name} -> 求解成功！")
        return True, res.q_arm

    def execute_pose(self, q_arm, time_sec):
        arm_msg = armTargetPoses(times=[time_sec], values=[math.degrees(q) for q in q_arm])
        self.arm_pub.publish(arm_msg)
        time.sleep(time_sec + 0.5)
        
    def trigger_tts_fallback(self, text):
        """直接请求下位机本机的 5000 端口，绕过大模型直接执行底层报错发声"""
        try:
            print(f"📢 触发物理层异常回退播报: {text}")
            requests.post("http://127.0.0.1:5000/tts", json={"text": text}, timeout=3)
        except Exception as e:
            print(f"⚠️ TTS 请求失败，发声器官未启动: {e}")

    # --- 大模型指令响应回调 ---
    def command_callback(self, msg):
        if self.is_working:
            rospy.logwarn("⚠️ 正在执行物理任务，已拦截并忽略大模型的新指令！")
            return
            
        try:
            cmd = json.loads(msg.data)
            if cmd.get("action") == "grab":
                target_name = cmd.get("target", "目标")
                self.is_working = True
                self.execute_grasp_pipeline(target_name)
        except Exception as e:
            rospy.logerr(f"大模型指令解析失败: {e}")

    # --- 核心抓取管线 ---
    def execute_grasp_pipeline(self, target_name):
        print(f"\n=======================================")
        print(f"🚀 接收大模型指令：开始抓取 [{target_name}]")
        print(f"=======================================\n")
        
        print("🖐️ 初始化双爪完全张开...")
        self.call_leju_claw([10.0, 10.0], [50, 50], [1.0, 1.0])
        time.sleep(1.0)
        
        print("🤖 机器手臂初始归位，锁定头部防晃动...")
        init_angles = [20.0, 0.0, 0.0, -30.0, 0.0, 0.0, 0.0, 20.0, 0.0, 0.0, -30.0, 0.0, 0.0, 0.0]
        self.execute_pose([math.radians(x) for x in init_angles], 2.5)
        self.head_pub.publish(robotHeadMotionData(joint_data=[0.0, 20.0]))
        time.sleep(1.5)

        # 🔥 超时熔断机制：最多等待 15 秒收集视觉数据
        print(f"\n👁️ 正在听取 TF2 绝对坐标 (目标: {target_name}，限时15秒)...")
        x_hist, y_hist = [], []
        start_time = time.time()
        
        while len(x_hist) < 10 and not rospy.is_shutdown():
            # 超时判定
            if time.time() - start_time > 15.0:
                print(f"❌ 视觉捕获超时！YOLO 未能在 15 秒内锁定 10 帧有效数据。")
                self.trigger_tts_fallback(f"抱歉，我没有在视野中看清{target_name}，抓取任务已取消。")
                self.is_working = False
                return
                
            try:
                msg = rospy.wait_for_message('/vla/yolo_target', PointStamped, timeout=0.5)
                if 0.30 <= msg.point.x <= 0.65:
                    x_hist.append(msg.point.x); y_hist.append(msg.point.y)
                    print(f"  📥 录入 ({len(x_hist)}/10): TF2_X={msg.point.x:.3f}, TF2_Y={msg.point.y:.3f}")
            except Exception: pass

        if len(x_hist) < 10: 
            self.is_working = False
            return

        raw_x, raw_y = np.median(x_hist), np.median(y_hist)
        is_left_arm = raw_y > 0.0
        active_arm = "左手" if is_left_arm else "右手"

        # 核心融合：TCP 物理补偿 + Z轴强锁
        locked_x = raw_x + TCP_OFFSET_X
        if is_left_arm: locked_y = raw_y + TCP_OFFSET_Y_LEFT
        else: locked_y = raw_y + TCP_OFFSET_Y_RIGHT
        locked_z = SAFE_LOCKED_Z  
        
        print(f"\n🎯 TF2 融合打击点: X={locked_x:.3f}, Y={locked_y:.3f}, Z={locked_z:.3f}")

        # IK 参数配置
        ik_req = twoArmHandPoseCmd()
        ik_req.use_custom_ik_param = True; ik_req.joint_angles_as_q0 = True
        ik_param = ikSolveParam()
        ik_param.major_optimality_tol = 1e-3; ik_param.major_feasibility_tol = 1e-3
        ik_param.minor_feasibility_tol = 1e-3; ik_param.major_iterations_limit = 500
        ik_param.pos_cost_weight = 0.0 
        ik_req.ik_param = ik_param

        target_quat = get_horizontal_claw_quat(locked_x, locked_y, is_left_arm)
        quat_array = [target_quat.x, target_quat.y, target_quat.z, target_quat.w]
        official_seed = [0.0, 0.0, 0.0, -1.57079633, 0.0, 0.0, 0.0]
        
        # === 1. 逆推规划 B (目标抓取点) ===
        ik_req.hand_poses.left_pose.joint_angles = official_seed
        ik_req.hand_poses.right_pose.joint_angles = official_seed

        if is_left_arm:
            ik_req.hand_poses.right_pose.pos_xyz = [-0.012, -0.225, -0.265] 
            ik_req.hand_poses.right_pose.quat_xyzw = [0.0, 0.0, 0.0, 1.0]
            ik_req.hand_poses.left_pose.quat_xyzw = quat_array
            ik_req.hand_poses.left_pose.pos_xyz = [locked_x, locked_y, locked_z]
        else:
            ik_req.hand_poses.left_pose.pos_xyz = [-0.012, 0.225, -0.265]   
            ik_req.hand_poses.left_pose.quat_xyzw = [0.0, 0.0, 0.0, 1.0]
            ik_req.hand_poses.right_pose.quat_xyzw = quat_array
            ik_req.hand_poses.right_pose.pos_xyz = [locked_x, locked_y, locked_z]
            
        ok, q_grasp = self.solve_ik(ik_req, f"步骤 B [{active_arm}终点切入]")
        if not ok: 
            self.trigger_tts_fallback("抱歉，目标位置超出了我的手臂物理极限，抓取取消。")
            self.is_working = False
            return

        # === 2. 逆推规划 A (进场直线预瞄点) ===
        ik_req.hand_poses.left_pose.joint_angles = list(q_grasp[:7])
        ik_req.hand_poses.right_pose.joint_angles = list(q_grasp[7:])
        robot_zero_x = -0.017; robot_zero_y = 0.292 if is_left_arm else -0.292
        dist = math.hypot(locked_x - robot_zero_x, locked_y - robot_zero_y)
        
        ratio = (dist - 0.12) / dist 
        pre_x = robot_zero_x + (locked_x - robot_zero_x) * ratio
        pre_y = robot_zero_y + (locked_y - robot_zero_y) * ratio

        if is_left_arm: ik_req.hand_poses.left_pose.pos_xyz = [pre_x, pre_y, locked_z]
        else: ik_req.hand_poses.right_pose.pos_xyz = [pre_x, pre_y, locked_z]
        ok, q_pre = self.solve_ik(ik_req, f"步骤 A [{active_arm}退后预瞄]")
        if not ok: q_pre = q_grasp

        # === 3. 逆推规划 D (垂直抬升起飞点) ===
        ik_req.hand_poses.left_pose.joint_angles = list(q_grasp[:7])
        ik_req.hand_poses.right_pose.joint_angles = list(q_grasp[7:])
        if is_left_arm: ik_req.hand_poses.left_pose.pos_xyz = [locked_x, locked_y, locked_z + LIFT_HEIGHT]
        else: ik_req.hand_poses.right_pose.pos_xyz = [locked_x, locked_y, locked_z + LIFT_HEIGHT]
        ok, q_lift = self.solve_ik(ik_req, f"步骤 D [{active_arm}垂直抬升 {int(LIFT_HEIGHT*100)}cm]")
        if not ok: q_lift = q_grasp 

        # === 4. 逆推规划 E (3D外扩避障通道：AI动态降级寻找撤退区) ===
        ik_req.hand_poses.left_pose.joint_angles = list(q_lift[:7])
        ik_req.hand_poses.right_pose.joint_angles = list(q_lift[7:])
        
        q_high_safe = q_lift
        for fallback_dist in [0.15, 0.12, 0.09, 0.06, 0.0]:
            ratio = (dist - fallback_dist) / dist if dist != 0 else 1.0
            safe_x = robot_zero_x + (locked_x - robot_zero_x) * ratio
            safe_y = locked_y + AVOID_OUTWARD_Y if is_left_arm else locked_y - AVOID_OUTWARD_Y
            
            if is_left_arm: ik_req.hand_poses.left_pose.pos_xyz = [safe_x, safe_y, locked_z + LIFT_HEIGHT]
            else: ik_req.hand_poses.right_pose.pos_xyz = [safe_x, safe_y, locked_z + LIFT_HEIGHT]
            
            ok, q_res = self.solve_ik(ik_req, f"步骤 E [外移{int(AVOID_OUTWARD_Y*100)}cm + 后退{int(fallback_dist*100)}cm]")
            if ok:
                q_high_safe = q_res
                print(f"💡 AI防撞系统：锁定 3D 避障点 (向外移{int(AVOID_OUTWARD_Y*100)}cm, 后退{int(fallback_dist*100)}cm)！")
                break

        # ================= 全部规划成功，开始物理闭环执行 =================
        print("\n🚀 所有航点验证通过！开始物理执行！")
        ready_angles = [40, 20, 0, -120, 0, 0, -20, 20, 0, 0, -30, 0, 0, 0] if is_left_arm else [20, 0, 0, -30, 0, 0, 0, 40, -20, 0, -120, 0, 0, -20]
        
        self.execute_pose([math.radians(x) for x in ready_angles], 2.5)

        print(f"✈️ 下降至预瞄点...")
        self.execute_pose(q_pre, 2.5)

        print(f"🎯 笔直平移插入目标...")
        self.execute_pose(q_grasp, 1.5)

        print(f"✊ 执行夹爪横向满力矩闭合...")
        if is_left_arm: self.call_leju_claw([90.0, 10.0], [50, 50], [1.5, 1.5])
        else: self.call_leju_claw([10.0, 90.0], [50, 50], [1.5, 1.5]) 
        time.sleep(2.0) 

        print(f"⬆️ 夺得目标，垂直拔高 {int(LIFT_HEIGHT*100)}cm...")
        self.execute_pose(q_lift, 2.0)

        print(f"⬅️ 向外侧平移避开下方物体 (3D L型走廊)...")
        self.execute_pose(q_high_safe, 2.0)

        print(f"🛡️ 执行关节曲肘折叠收手，确保不碰任何下方物体...")
        self.execute_pose([math.radians(x) for x in ready_angles], 3.0)

        print(f"🤖 恢复自然下垂原状...")
        self.execute_pose([math.radians(x) for x in init_angles], 3.0)
        
        print("👐 任务完成，松开夹爪释放目标...")
        self.call_leju_claw([10.0, 10.0], [50, 50], [1.0, 1.0])
        time.sleep(2.0)
        
        print("🎉 物理抓取流程圆满通关，状态机解锁等待新指令。")
        self.is_working = False

if __name__ == '__main__':
    daemon = VLAGraspStateMachine()
    rospy.spin()

```