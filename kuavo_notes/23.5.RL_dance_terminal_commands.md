# 🚀 S49 四代机 IL+RL 全身舞终端命令与全栈工仓流向指南

---

## 📌 前情提要与底层运行逻辑

在人形机器人强化学习的真实上机和仿真部署调试中，**在复制粘贴任何一行终端命令前，必须彻底理解其背后的力学与软件假设（WHY）**——否则你将陷入严重的恶性死锁怪圈：Kit 引擎无故挂起、显存爆掉 CUDA OOM、或者模型导出后在 MuJoCo 仿真里一接管就摔碎。届时你只能盲目试错。

本篇指南将从最初的环境清洗、轻量化资产洗刷，到中盘的 PPO 炉火训练、网络 ONNX 导出，再到后盘的 Docker WBC WBC 节点全量重编、MuJoCo 仿真验证，乃至最后的真机点火发车，将全栈所有的原子级命令行按照**五大端到端闭环工作流**进行无损串联。

* **最高版本对齐红线**：训练引擎沙盒端、MuJoCo 仿真验证端、真实下位机真机点火端，三端命令执行前全量强制锁定统一配置 `export ROBOT_VERSION=49`。
* **显存硬件极限生存基准**：针对主流笔记本独立显卡（如 RTX 3070 Laptop 8GB），训练并发数极限焊死在 `--num_envs=2048`；大工作站 24GB 显存设备方可上调至 8192。

---

## 第 0 章补：五段式端到端工作流总览（Train → Real）

在复制任何单行命令之前，请先建立这条**不可跳步**的总链路；跳步是 MuJoCo 挺尸与真机摔机最常见的根因：

```text
[0 清洗] pkill kit + unset DISPLAY + setup_s49_training_assets.sh + rm /tmp/IsaacLab/usd_*
    ↓
[1 训练] train.py  S49-Punch-v0  num_envs=2048  →  logs/.../params/env.yaml 为金标准
    ↓
[2 导出] play.py  S49-Play-v0  →  exported/policy.onnx  →  手动 cp 为 policy_s49.onnx
    ↓
[3 编译] Docker 内 catkin clean && catkin build humanoid_controllers
    ↓
[4 Sim2Sim] ROBOT_VERSION=49  roslaunch mujoco dance_s49  →  R 键  →  30s 验收（23.4 §7.1）
    ↓
[5 真机] 挂绳 SOP + load_kuavo_real.launch + F 拨杆 WBC → B 键 RL（仅 Sim2Sim 通过后）
```

---


## 第 0 章：一次性环境清洗与资产洗刷管线（新机或更换资产必做）

以下原子级命令只需在新下位机工作站首次部署、或者你的机器人更换了全新的原始 URDF 资产结构后执行一次。**严禁直接跳过此章，否则你的系统在开局阶段要么会报出 import 崩溃，要么会因为 48 关节全自由度未裁剪而引发 Action/Obs 空间发生灾难性的维度大爆炸。**

### 0.1 筑起 Isaac 训练环境前置防爆盾牌护城河

在进行 headless 无界面极限压榨训练前，必须用断头铡式强制清理掉后台所有残留的英伟达 Kit 僵尸进程、拔除 X11 图形界面显示锁、并全量注入路径环境变量。

#### 0.1.1 为什么必须铁血执行 `pkill kit`？

如果上一次训练因为意外断流或者中途报废退出了，Ubuntu 后台会非常顽固地残留下一堆由 Omniverse 派生出的 `kit` 僵尸守护进程。这些进程会死死霸占着 GPU 显存池不释放，并对底层的物理缓存锁（Cache Lock）文件进行死锁——这会导致你开启新一炉训练时，程序永远卡死在开局的 Splash Screen 黑屏加载线处弹不出 Progress 图表。

#### 0.1.2 为什么必须在 Headless 炼丹前执行 `unset DISPLAY`？

在 Headless 盲跑模式下，如果系统的 `DISPLAY` 环境变量依然顽固指向宿主机的 X11 转发端口，Kit 引擎会因为强行去进行 GLFW 图形视窗初始化而发生冲突挂起死锁。请保持绝对冷静：**在 Headless 模式下，控制台日志滚动弹出的 `GLFW initialization failed` 警告，完全是剥离图形管线时的正常力学排异日志，不是报错，直接忽略即可。**

#### 0.1.3 为什么更换 URDF 后必须暴力清空 `kit/cache` 临时缓存目录？

Kit 内部挂载了极其顽固的 USD 资源记忆缓存。如果你修改或精简了机器人的 URDF 骨骼，但没有去清空临时缓存，PhysX 求解器在开局第 1 帧依然会固执地去内存里加载上一轮旧模型的 48 关节错误资产，直接导致 Action 空间在训练开局两秒内崩溃解体。

直接在你的宿主机标准 Bash 环境下，复制并砸入以下全套清洗与环境变量注入连招：

```bash
# 1. 物理级强杀残留僵尸进程，砸碎 Kit 缓存锁
pkill -9 -f kit
pkill -9 -f python
rm -rf ~/.local/share/ov/pkg/isaac-sim-4.2.0/kit/cache/*

# 2. 激活 Conda 隔离环境，彻底拔除 DISPLAY 图形显示锁
conda activate isaaclab
cd ~/kuavo_all/leju_robot_rl
unset DISPLAY

# 3. 注入英伟达原厂硬核环境变量总线（WANDB强制离线）
source ~/.local/share/ov/pkg/isaac-sim-4.2.0/setup_python_env.sh
export EXP_PATH=$HOME/IsaacLab/source/apps/isaaclab.python.headless.kit
export CARB_APP_PATH=$HOME/.local/share/ov/pkg/isaac-sim-4.2.0/kit
export ISAAC_PATH=$HOME/.local/share/ov/pkg/isaac-sim-4.2.0
export VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json
export WANDB_MODE=offline

```

* **健康运行期望判据**：后续执行 `train.py` 时，系统没有任何 import 语法报废红字，控制台前 5 行顺利滚动出真实的 Isaac Sim 官方标称版本号。
* **排卡 Troubleshooting 门哨**：如果提示 `which python` 依旧指向系统原生路径，说明你刚才粘贴命令时发生了 Conda 环境变量被 Zsh 插件截断的意外，请重新打点执行一遍 `conda activate isaaclab`。

### 0.2 四代机专属 26 DoF 轻量化资产（`lite URDF`）生成管线

运行此流水线，将官方原厂肥大的 48 关节资产剔除，物理级焊死多余的手指和头部节点。

```bash
cd ~/kuavo_all/leju_robot_rl
# 1. 一键启动全自动资产洗刷打包机
bash scripts/tools/setup_s49_training_assets.sh

# 2. 强行肉眼核对资产是否在对应沙盒文件夹下完美无损生成
ls exts/ext_template/ext_template/assets/Robots/Kuavo/s49_train/urdf/biped_s49_26dof_lite.urdf

```

* **高危预警与物理清空指令**：正如前文所述，为了彻底杜绝新旧骨架 USD 资产在 PhysX 内部发生恶意踩踏错位，改完 URDF 资产的绝对第一秒，**必须无条件在终端执行强制清空命令，砸碎由于跨代模型残留下的旧代 USD 缓存锁**：
```bash
rm -rf /tmp/IsaacLab/usd_*

```

* **健康运行期望判据**：`ls` 指令反馈文件确凿存在，调用 `du -sh` 核对该 lite URDF 的文件体积处于正常的非空、非零损坏、合法的 XML 节点语义状态。

### 0.3 跨代舞蹈 CSV 轨迹源高频重采样管线

当你需要使用最新的官方五代长编舞数据作为四代机 S49 满血长周期的动作输入时，拉起此数据重采样管线：

```bash
python3 scripts/tools/adapt_s54_dance_to_gen4.py

```

* **输出黄金真理文件**：这会在动作工仓内生出长达 1565 帧、完全剥离了 `waist_yaw` （腰部旋转自由度）的纯净 26 列弧度制（rad）文件：`kuavo_action_S49_FROM_S54_INPLACE_RAD.csv`。其对应在部署端必须配置的周期常数为 `cycleTime=31.30s`。如果提示缺少 Pandas，请在容器或环境里执行 `pip install pandas`。

### 0.4 打通两端物理资产的软链接桥接桥梁

训练包里的 URDF 放在 `leju_robot_rl` 仓下，而 MuJoCo 验证包与真机启动节点统一从 `kuavo-rl-opensource` 部署路径下读取参数。必须执行以下软链接强行桥接命令，将两端的刚体惯量网格达成 100% 绝对物理孪生：

```bash
ln -sfn ~/kuavo_all/kuavo-ros-opensource/src/kuavo_assets/models/biped_s49 \
  ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy/src/kuavo_assets/models/biped_s49

```

---

## 第一章：原子级 PPO 仿真训练启动与监控

在每次执行 `train.py` 开启炉火前，必须无条件再次刷新执行一次**第 0.1 节里规定的全套环境清洗防爆盾牌护城河连招**，将残留的 Kit 碎步和图形显示锁全面阻断清除！

### 1.1 针对 8GB 显存笔记本独立显卡的极限生存点火命令（2048 envs）

如果你的硬件是一张 RTX 3070 笔记本独显，英伟达仿真引擎底层在运行时会吞掉近 `7.25GB` 的极限死区显存。请将并行大军数规整限定在 **`2048`**，这是能够保证在 8GB 卡上绝对不会触发 CUDA OOM 显存崩溃的黄金最高安全上限！

```bash
python3 scripts/rsl_rl/train.py --task Legged-Isaac-Velocity-Flat-Kuavo-S49-Punch-v0 --num_envs 2048 --headless

```

### 1.2 针对大工作站 24GB 显存设备的豪奢对比点火命令（8192 envs）

该配置专属于大显存服务器设备，四代机 S42/S46 阶段曾采用过此架构作为历史对照：

```bash
python3 scripts/rsl_rl/train.py --task Legged-Isaac-Velocity-Flat-Kuavo-Punch-v0 --num_envs 8192 --headless

```

* **监控过滤排查哨**：回车敲下后的前 30 秒，必须死死盯紧控制台打印出来的结构数据长表格：**确认 `Action Terms (shape: 26)` 已经干净对齐；确认 `policy obs (shape: 115)` 已经扩维成功**！如果惊悚地看到 Action 依然显示为 48 维或 Obs 显示为 181 维，说明资产洗刷失败，立刻按下 `Ctrl+C` 断流强退，重新去执行一遍 `rm -rf /tmp/IsaacLab/usd_*` 缓存砸碎命令。

### 1.3 启动本地 TensorBoard 监控大雷达

训练大军跑起来后，无需去盲目猜测收敛情况，直接新开一个外部终端标签页，高频拉起 TensorBoard 监控雷达：

```bash
tensorboard --logdir ~/kuavo_all/leju_robot_rl/logs/rsl_rl/Kuavo/s49/dance --port 6006

```

打开你的 Chrome 浏览器，访问本地物理端口 `http://localhost:6006`。时刻交叉校验 `flat_orientation_l2`（表征整体机身防偏斜跌倒惩罚项指标）的负分曲线是否已经**成功向上逆转、收敛并死死闭环在零刻度水平带附近**；一旦发现该曲线常年深陷在极其危险的低负分死区，说明智能体再次走入了“趴在地上挥手刷分”的作弊歪路，请果断清理权重，重修奖励机制再开炉。

---

## 第二章：离线策略回放与 115维满血 ONNX 大脑重组导出

策略在 9000 步左右收敛定型后，我们需要使用专属的 `-Play-v0` 环境将 PyTorch 格式的 `.pt` 炼丹权重文件，转换并重组导出为部署专用的 `.onnx` 二进制文件。

### 2.1 考虑了 115 维高高维重排外壳的全新 `play.py` 导出指令

```bash
python3 scripts/rsl_rl/play.py --task Legged-Isaac-Velocity-Flat-Kuavo-S49-Play-v0 --num_envs 32 --headless --load_run 2026-06-13_04-59-23 --checkpoint model_9000.pt

```

* **命令细节微调规范**：其中的 `--load_run` 后的时间戳字符串，必须去你本地的 logs 目录里查看并**精准替换为你那一炉最成熟的训练文件夹的真实时间戳名字**！
* **模型改名红线机制**：因为 play.py 脚本在底层默认只会机械性地在对应的 `exported/` 目录下吐出一个名字固定的通用文件 `policy.onnx`。而我们的 4.6 版本的部署控制器在读取网络时，只认你统一大一统指定的专属高维名字。**必须在模型生成完毕后，用最快的速度手动敲入 Linux 改名拷贝命令，将其强行伪装、改名并全量覆盖到下位机部署包指定的网络神经中枢目录下：**
```bash
cp logs/rsl_rl/Kuavo/s49/dance/2026-06-13_04-59-23/exported/policy.onnx \
   ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy/src/humanoid-control/humanoid_controllers/model/networks/policy_s49.onnx

```

* **技术断代警示**：**绝对禁止继续沿用历史遗留下来的、带有旧切片错误语义的 `policy_s42.onnx` 文件！** 因为在完成 `feat/punch` 分支的 115 维 Upright 重构门控手术后，网络的输入神经元排布完全被重写。错用旧 ONNX，MuJoCo 会在启动瞬间发生 map::at 内存越界暴毙。

* **部署 ONNX 文件名**：控制器加载 **`policy_s49.onnx`**（数字 **49**）。上文 cp 示例如时间戳不同请替换 run 目录；再拷贝一次可用：

```bash
cp logs/rsl_rl/Kuavo/s49/dance/2026-06-13_04-59-23/exported/policy.onnx    ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy/src/humanoid-control/humanoid_controllers/model/networks/policy_s49.onnx
```

* **同步拷贝舞蹈 CSV 到部署 config 目录**（与训练 `DANCE_CSV` 同源）：

```bash
cp ~/kuavo_all/leju_robot_rl/kuavo_action_S49_FROM_S54_INPLACE_RAD.csv \
   ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy/src/humanoid-control/humanoid_controllers/config/kuavo_v49/rl/kuavo_action_S49_FROM_S54_INPLACE_RAD.csv
```

> **【原文存档】** 上段「技术断代警示」已完整收录于「文件名勘误」之前；本行原为插入锚点遗留的重复正文，保留作对照。

---

## 第三章：Docker 容器内的 C++ 控制节点 WBC WBC 状态机全量重新编译

策略ONNX大脑文件覆盖完成。由于我们的时间轴推移时钟、`referenceJointPos` 电机硬件序重排置换矩阵以及物理 `ankleSolver` 旁路（bypass）注销指令全部书写在控制仓的 C++ 源码文件 `humanoidController.cpp` 里面。**任何一处代码的微调，必须且必须要强行切入 Docker 容器内部执行全量重新编译，不编译重刷缓存，MuJoCo 的行为完全是在盲目抽卡！**

### 3.1 宿主机与 Docker 容器内部运行环境的路径映射大桥

* **实验室标准映射路径**：宿主机目录 `~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy` $\Longleftrightarrow$ 强行映射挂载进入 Docker 容器内部的主工作区 `/root/kuavo_ws`。

请直接在你的宿主机标准外层终端里，敲入连招，物理级长驱直入杀入 Docker 容器的最深处：

```bash
# 1. 在宿主机后台唤醒并强势合体接入编译容器（镜像名以现场机器实际打包标定为准）
docker start kuavo_deploy && docker exec -it kuavo_deploy bash

# 2. 切入 C++ ROS 控制器的核心工作空间
cd /root/kuavo_ws

# 3. 【最核心步骤】：铁血无情地强制清理掉之前官方走路模型遗留下的所有旧代缓存记忆！
catkin clean -y
source installed/setup.zsh

# 4. 启动多线程满负荷全量重新编译控制核心节点包
catkin build humanoid_controllers

```

* **健康编译期望判据**：终端刷出白色的 `Summary: All 14 packages succeeded!` 成功归档字样，无任何红字 Error 阻碍。如果爆出找不到 `kuavo_msgs`，说明你在编译前忘记了执行 `source installed/setup.zsh` 数据总线激活操作，重新打点source一遍即可秒杀报错。

---

## 第四章：MuJoCo Sim2Sim 仿真验证端点火验证与 ROS 扰动测试

两端数据彻底焊死。接下来就是最激动人心的 Sim2Sim 线上验证大拉练。

### 4.1 启动专属于 S49 全身协调舞蹈的 MuJoCo 物理节点

请在你的 Docker 容器终端内（或者是在开辟了 devel 空间总线映射的宿主机干净终端内），敲下发令枪：

```bash
export ROBOT_VERSION=49
cd ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy
source devel/setup.zsh
roslaunch humanoid_controllers load_kuavo_mujoco_sim_dance_s49.launch joystick_type:=sim

```

* **操作考核连招规程**：等待 2 秒钟，直到 MuJoCo 仿真器黑屏视窗成功跳出，且其顶部的 **`Run` 状态指示灯成功变绿**！当机器人双脚踩稳、顺畅执行完 WBC 的下蹲和起立名义静态平衡动作之后——操作手深吸一口气，**在键盘上果断敲下解锁 RL 模式的控制权交接键：点按 `R` 键**（与 **23.1**、**23.4** Sim2Sim 叙述一致；`joystick_type:=sim` 时以终端键盘映射为准）！

### 4.2 终极离线物理快照查数命令（照妖镜）

按键下发后，机器人应瞬间以 100/150 物理孪生大刚度死死扎稳马步撑住重心，并自如操控手臂舞动。如果不放心，立刻新开终端，砸入本命令查数：

```bash
rostopic echo /rl_controller/singleInputData -n 1

```

* 如果特征总数完美显示为 `115`，说明 115 维满血字典成功咬合；如果 `commandPhase` 的 sin/cos 在随着时间轴高频滚动，说明 `cmdStance=0` 推进大门完好放行；如果 `gravity_body` 读数稳在 `[0,0,-1]`，说明欧拉角内鬼被彻底清除！

### 4.3 工业级 ROS topic 话题抗扰动冲击“盲推测试”连招

不要用鼠标去拉扯视窗。直接在外部宿主机新开终端，**砸入这行由最干净英文单引号 `''` 严格包裹 YAML 消息体的 80N 强力侧向推搡脉冲指令**：

```bash
rostopic pub /external_wrench geometry_msgs/Wrench '{force: {x: 0.0, y: 80.0, z: 0.0}, torque: {x: 0.0, y: 0.0, z: 0.0}}' -r 50

```

观察窗口里的 Kuavo 机甲是否展现出极其强悍的闭环力矩代偿。观察完毕后，**必须通过这行原子命令下发一帧干净的零输入，方可平稳撤掉外力，防止机器人由于力矩过载死锁而被永远推飞出宇宙：**

```bash
rostopic pub /external_wrench geometry_msgs/Wrench '{force: {x: 0.0, y: 0.0, z: 0.0}, torque: {x: 0.0, y: 0.0, z: 0.0}}' -1

```

---

## 第五章

### 4.4 Sim2Sim 30 秒最低验收（命令层复述）

Sim2Sim 章节操作完成后，在 **未** 发布 `/external_wrench` 时，机器人须 **≥30 秒** 满足：**115 维观测正常**、**commandPhase 滚动**、**无挺尸/劈叉/侧移**、**挥拳节律与 CSV 一致**。详细判据与可选抗扰步骤见 **23.4** §7.1。未通过则返回 **23.4** P0 金字塔排查，**禁止**进入本章真机 launch。

---
：真机部署的最高安全防线与极高频故障断路器速查手册

只有当你的机器人在 MuJoCo 仿真中能够稳健地独立原地跳舞打拳 **长达 30 秒以上且无任何歪斜、无任何侧移、无任何反关节劈叉** 的大前提下，我们才真正恩准并考虑将策略移步推向最危险、最昂贵的物理真机战场！真机由于具备真实的摩擦力和减速器过载，其危险度高出仿真十倍。

### 5.1 物理真机带电发车完整点火指令（SOP操作）

请将这台 58.2kg 的钢铁猛兽牢牢悬挂卡紧在承重龙门架保护钢丝拉紧绳上。

```bash
export ROBOT_VERSION=49
cd ~/kuavo_all/kuavo-rl-opensource/kuavo-robot-deploy
source devel/setup.zsh
# 🚀 真实物理世界全量点火发车命令，指定硬件 H12 手柄作为最高接管载体
roslaunch humanoid_controllers load_kuavo_real.launch joystick_type:=h12

```

#### 🛠️ 实机接管手势操作纪律

1. 打开 launch 命令后，确保机器人的承重防护绳索只在贴地落地后**保持极其吝啬的 2~3 cm 微量松弛余量弯曲度**，坚决隔离冲击外力干扰。
2. 抢险断电急停爆破护卫人员双脚死死卡死站立在机器人后背发力的 `45° 侧后方黄金避险盲区盲点`，手指悬空停留在距离物理断电红色蘑菇头急停按钮半米以内的绝对冲刺抢救范围内！
3. 主遥控手握紧 H12 遥控器，**首先果断将 `F 拨杆`死死拉满推至最顶点**，命令原厂 WBC 起立状态机强力接管，托举 Kuavo 稳稳笔直直立站好；在肉眼和耳朵仔细辨识、确认减速器无任何高频异响尖叫、整机姿态极度收敛平稳的两秒钟之后——操作手深吸一口气，**极其快速敏捷地单次重力点按一下手柄上的红色 `B 键` RL 接管核弹按钮**！此时，属于你全量手搓、115维对齐的 policy 舞蹈动作将正式全面降临在这具钢铁躯体之上！

---

### 🚨 终极核弹级：全栈开发部署极高频失败现象断路器速查表

当你在任何一步遭遇崩溃，请直接查阅本表，下狠手精准阻断：

| 故障代码与堆栈表象 | 故障背后暗藏的底层骨骼病理根因 (WHY) | 我们的全栈行级行级手术切除解药方案 (HOW) |
| --- | --- | --- |
| **💥 F1: 训练开始 3 秒内直接抛出致命 CUDA OOM 显存大血崩崩溃** | 你在一张可怜的 8GB 显存显卡上盲目在命令里下达了豪奢的 `--num_envs 8192`，导致计算图瞬间挤爆显存物理池。 | 认清硬件瓶颈现实，在启动命令里主动将大军规模强行降频阉割砍掉 4 倍，修改回标准的 **`--num_envs 2048`** 稳健生存线。 |
| **💥 F2: 换完 URDF 之后重训， Action 依然显示为 48 维，或者 Obs 依旧停留在 181 维发散** | 顽固的英伟达 Kit 引擎内部对旧模型产生了恶性的缓存死锁记忆，导致 PhysX 求解器根本没去读你全新的 lite 资产。 | 训练点火命令前，必须在外部 Bash 终端里强制砸入物理清空断头铡命令：**`rm -rf /tmp/IsaacLab/usd_*`** 彻底扬飞碎片！ |
| **💥 F3: 仿真拉起一切正常，但只要键盘一按 R 键解锁，机器人瞬间发生可怕的“一字马大劈叉”解体抽搐** | 部署侧的 C++ 状态机在从 CSV 表格里向外提取 `referenceJointPos` 动捕期望时，**非常愚蠢地误调用了正向重排矩阵 gym2lab 导致关节角度全量对错通道**。 | 移步至 `humanoidController.cpp` 的重组模块，**确认其逆变换常数数组已经通过硬编码手段与 play.py 导出的 `lab2gym` 矩阵达成 100% 绝对一致**。 |
| **💥 F4: 机器人能跳舞，但极度生涩且时间轴完全错拍子，动作慢了整整十倍** | 部署包里的 `cycleTime` 参数仍遗留了原厂走路步伐的 1.2s 错误配置，逼迫下位机状态机以快了 10 倍的超频速度在脑海里疯狂刷新拍子。 | 打开 `skw_rl_param_dance.info` 顶部，**根据你的 CSV 真实总行数，将其强制覆写修改对齐为：642帧 $\rightarrow$ 12.84s 或 1565帧 $\rightarrow$ 31.30s**。 |
| **💥 F5: 按 R 键解锁后，机器人在 MuJoCo 里表现出大面积死锁、手臂僵直毫无任何动作刷新响应** | 你在参数卡里误保留了官方走路模型的 **`cmdStance=1` 隐藏冻结参数**，这导致底层的连续时间轴相位推进主时钟在第 1 帧就被强行填零死锁。 | 进入 `skw_rl_param_dance.info` 顶部，**将 `cmdStance` 默认初值在发车前强制修改写入为零：`cmdStance 0`。 |
| **💥 F6: 仿真拉起一按 R 键解锁接管，机器人像被抽掉脊髓一样，毫无任何红字报错、极其安静地往后瞬间拍倒挺尸** | 你踩中了整个 Sim2Sim 最致命的数据错位大坑：参数卡里 singleInputData 里的**子字典键名书写上下排版次序彻底写反了**，神经网络接到了 Roll-Pitch-Yaw 欧拉角乱码欧拉角。 | 打开 `skw_rl_param_dance.info` 底部，**全量按照原厂 A-Z 字母表递增顺序重写 singleInputData 排版，并无条件将所有 startIdx 全部改回原厂连续的全 0 状态**！ |
| **💥 F7: 在 Zsh 终端下输入 ROS 话题推搡命令，控制台疯狂刷屏抛出大面积 `{}` 语法报废红字报错** | 这是一个极为低级但高频发生的 Linux 终端冲突错误。由于你误用了双引号 `""` 导致 Zsh 的大括号展开插件（brace expansion）被恶意激活，撕碎了你的 YAML 消息。 | 保持钢铁纪律：**在发布任何 rostopic pub 命令时，外层的受力消息体字符串，必须、只能、无条件使用最干净的英文单引号 `''` 进行全量包裹！** |
| **💥 F8: 宿主机上改完了 `humanoidController.cpp` 的映射源码，但重新 roslaunch 后机器人的劈叉/侧移毛病毫无任何变化** | 这是一个典型的由于缺少 build 导致的缓存僵死。你仅仅修改了外部的 config，由于没有去 Docker 内部刷编译，导致系统运行时依然在固执读取旧的二进制缓存。 | 立刻切回 Docker 容器 `/root/kuavo_ws` 目录下，**铁血无情地强制执行全量重编大连招： `catkin clean -y && catkin build humanoid_controllers` 刷新缓存**！ |

---
